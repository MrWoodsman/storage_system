export const PATHS = {
    HOME: '/',
    BOXES: '/boxes',
    ITEMS: '/items',
    SETTINGS: '/settings',
};

// Nie potrzebne
// Funkcje do generowania dynamicznych ścieżek
// export const getBoxDetailPath = (boxId) => `${PATHS.BOXES}/${boxId}`;